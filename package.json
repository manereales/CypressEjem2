{
  "name": "cypress_eje1",
  "version": "1.0.0",
  "description": "proyecto de pruebas practica cypress",
  "main": "index.js",
  "scripts": {
    "test": "cypress open",
    "chrome": "cypress run --spec cypress/e2e/SmokeTestBanco.cy.js --browser chrome",
    "firefox": "cypress run --spec cypress/e2e/SmokeTestBanco.cy.js --browser firefox",
    "edge": "cypress run --spec cypress/e2e/SmokeTestBanco.cy.js --browser edge"
  },
  "author": "Manuel Reales",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.5",
    "cypress-plugin-tab": "^1.0.5"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.5",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "^13.2.0",
    "cypress-file-upload": "^5.0.8",
    "cypress-xpath": "^2.0.1",
    "esbuild": "^0.19.3"
  }
}
